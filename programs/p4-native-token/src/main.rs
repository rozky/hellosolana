fn main() {
    // the main code is located in test folder as that allows me to have multiple
    // "programs/examples" within same setup otherwise I would need to create separate
    // program for each example
}